// 通过if判断状态
function showResult (result) {
  if (result === 0) {
    // 状态0处理程序
  } else if (result === 1) {
    // 状态1处理程序
  } else if (result === 2) {
    // 状态2处理程序
  } else if (result === 3) {
    // 状态3处理程序
  }
}
